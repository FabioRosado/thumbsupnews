version: '3.7'

services:
  db:
    image: postgres
  web:
    build:
      context: .
      dockerfile: ./Dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/thumbsupnews
    ports:
      - "8000:8000"
    image: thumbs_up_news
